# Created by Atakan Çetinkaya | 24.04.2023 | This is the finished product YAML File! |
# Version: v0.1
# This YAML creates a ConfigMap (cm), Service (svc), Deployment and a Pod.
# It is creating all important Components to access the ML Tool's.
# Last Edited Date/Time: 24.04.2023/10:22:59
# # # This is the test yaml file, here im going to test this yaml and continue to develop it
apiVersion: v1
kind: ConfigMap
metadata:
  name: pytorch-config
  namespace: pytorch
data:
  default.conf: |
    server {
      listen 8082;
      server_name localhost;
      location / {
        proxy_pass http://pytorch-service:8082;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      }
    }
---
apiVersion: v1
kind: Service
metadata:
  name: pytorch-service
  namespace: pytorch
spec:
  selector:
    app: pytorch
  ports:
    - name: http
      port: 80
      targetPort: 8082
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: pytorch-deployment
  namespace: pytorch
spec:
  replicas: 1
  selector:
    matchLabels:
      app: pytorch
  template:
    metadata:
      labels:
        app: pytorch
    spec:
      containers:
        - name: pytorch
          image: pytorch/pytorch:latest
          command: ["python"]
          args: ["-m", "http.server", "8085"]
  strategy:
    type: Recreate
---
apiVersion: v1
kind: Pod
metadata:
  name: pytorch-nginx
  namespace: pytorch
spec:
  containers:
    - name: nginx
      image: nginx
      ports:
        - containerPort: 8082
      volumeMounts:
        - name: pytorch-config
          mountPath: /etc/nginx/conf.d/default.conf
          subPath: default.conf
    - name: pytorch
      image: pytorch/pytorch:latest
      command: ["python"]
      args: ["-c", "import torch; import time; time.sleep(3600);"]
  volumes:
    - name: pytorch-config
      configMap:
        name: pytorch-config
        items:
          - key: default.conf
            path: default.conf
